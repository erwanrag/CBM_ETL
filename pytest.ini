# pytest.ini
[pytest]
markers =
    unit: Tests unitaires (pas de DB)
    integration: Tests nécessitant DB SQL/Progress
    slow: Tests lents (>5s)

# Ignorer warnings
filterwarnings =
    ignore::UserWarning
    ignore::FutureWarning